$kind: http-request
method: POST
url: '{{baseUrl}}/claims/:id/submit'
order: 7000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
  - key: X-Idempotency-Key
    value: '{{$randomUUID}}'
pathVariables:
  - key: id
    value: '{{$randomUUID}}'