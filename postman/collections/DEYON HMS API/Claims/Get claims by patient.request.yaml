$kind: http-request
method: GET
url: '{{baseUrl}}/claims/patient/:patientId'
order: 10000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
pathVariables:
  - key: patientId
    value: '{{$randomUUID}}'