$kind: http-request
method: GET
url: '{{baseUrl}}/departments/:id/staff'
order: 6000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
pathVariables:
  - key: id
    value: '{{$randomUUID}}'