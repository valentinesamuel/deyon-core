$kind: http-request
method: GET
url: '{{baseUrl}}/shifts/staff/:staffId'
order: 6000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
pathVariables:
  - key: staffId
    value: '{{$randomUUID}}'