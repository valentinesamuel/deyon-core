$kind: http-request
method: GET
url: '{{baseUrl}}/shifts/department/:departmentId'
order: 7000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
pathVariables:
  - key: departmentId
    value: '{{$randomUUID}}'