$kind: http-request
method: GET
url: '{{baseUrl}}/appointments/doctor/:doctorId'
order: 8000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
pathVariables:
  - key: doctorId
    value: '{{$randomUUID}}'