$kind: http-request
method: GET
url: '{{baseUrl}}/lab/catalog/:id/reference-ranges'
order: 5000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
pathVariables:
  - key: id
    value: '{{$randomUUID}}'