$kind: http-request
name: Bootstrap system
method: POST
url: "{{baseUrl}}setup/bootstrap"
order: 3000
headers:
  Authorization: Bearer {{token}}
body:
  type: json
  content: |-
    {
        "totpCode": "740117"
    }
examples: ./.resources/Bootstrap system.resources/examples
