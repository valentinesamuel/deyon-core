$kind: http-request
method: GET
url: '{{baseUrl}}/notifications/user/:userId'
order: 5000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
pathVariables:
  - key: userId
    value: '{{$randomUUID}}'