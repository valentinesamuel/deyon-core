Arrange advanced filtering

I want to work a very robust, scalable, fast, extendabale and maintainble, optimized battle tested and production fit query engine for my app. when i am trying to get all endpoints like get all staffs, get all appointments, etc.  For example, i want to be able to do filter[parent.author]=john, filter[user.company.department.country]=USA, handle self refrential relationships, polymorphic relationships, eq, ne, gt operators, filter[activity_date][gte]=2024-01-01&filter[activity_date][lte]=2024-12-31, case insensitive search on any column like search[title]=morning, security & whitelisting, auto joins, ability to select the columns that the client needs instead of everything being returned and  other query params like pagination, filtering. Don't write any code yet. Just architect the high level solution, measures to put in place, features that are nice to have, and features that are required. It should be able to handle at least 1000 - 2000 RPS with low latency. Do not assume anything, ask questions if you need clarity on the requirements