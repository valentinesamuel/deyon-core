# name: Load Tests

# on:
#   workflow_dispatch:
#     inputs:
#       target_url:
#         description: 'Target URL for load testing'
#         required: true
#         default: 'http://localhost:3000'
#       scenario:
#         description: 'k6 scenario to run'
#         required: true
#         type: choice
#         options:
#           - login-flow
#           - token-refresh
#           - role-protected
#         default: login-flow

# jobs:
#   load-test:
#     name: k6 Load Test - ${{ github.event.inputs.scenario }}
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Run k6 load test
#         uses: grafana/k6-action@v0.3.1
#         with:
#           filename: test/load/scenarios/${{ github.event.inputs.scenario }}.k6.ts
#         env:
#           TARGET_URL: ${{ github.event.inputs.target_url }}

#       - name: Upload k6 results
#         uses: actions/upload-artifact@v4
#         if: always()
#         with:
#           name: k6-results-${{ github.event.inputs.scenario }}
#           path: k6-results/
